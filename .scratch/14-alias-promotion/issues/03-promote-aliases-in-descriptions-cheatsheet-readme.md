# 宣传面：速查表别名列、description 提及、README 补齐

**Status:** ready-for-agent

**Blocked by:** 02（add-seven-aliases-with-guardrails）——需先确定别名全集（既有 + 7 新），宣传内容才能一次性对齐；另对 13（速查表）与 08（description 精简）存在跨目录依赖（见下）

**构建内容：** AI 在读取工具描述、速查表、README 时，能系统性看到可用别名：速查表每行有完整别名列，有别名工具的 description 在精简改写时以"别名：xx"一笔带过，README 补齐新别名行。消除"AI 不知道别名存在、每次打全名"的不可见问题，让别名真正可被采用。

**验收标准：**

- [ ] 速查表（13 号工单产物）新增/补全别名列，收录别名全集（既有 16 组 + 7 新），以 13 的护栏对账结果为准，逐行对齐
- [ ] 有别名工具的 description 在 08 号工单精简改写时，对高频别名以"别名：xx"一笔带过（受 token 预算约束，不全量罗列，取舍符合 08 精简口径）
- [ ] README 保持既有内联标注风格（如 `find`/`cat`/`ping` 的既有行），补齐新别名所在行的内联标注
- [ ] 与 13/08 的跨目录协调完成：本工单确认 13 的别名列数据源与 08 的"别名：xx"文案口径，无重复定义、无口径冲突
- [ ] PRD-07 中"cat→text_cat"的口径错误在本目录任何后续产出中不复现——正确读法为"`ls`→`fs_list`（别名→正名）"，`cat` 是正名、`text_cat` 是别名
- [ ] 全部宣传面描述/文案经 `findTool` 解析验证：文中每个别名确实可解析到正名，无"宣传了不存在的别名"

**测试 seam：** 本工单主要为文档/文案层，涉及元数据时以 `findTool(alias)` 或 `listTools()` 断言作为可解析性校验；不新增工具行为测试。

**评论：**

- 本工单对 13（速查表）与 08（description 精简）有**跨目录依赖**：13 号工单的别名列数据源、08 号工单的"别名：xx"文案口径需与本 PRD 对齐；实施前应与 13/08 的负责 agent 协调确认。
- 只改文档/描述/README，不改任何工具行为与注册表。
