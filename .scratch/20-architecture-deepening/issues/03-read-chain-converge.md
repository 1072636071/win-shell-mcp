# 03 · 读文件链路收敛深模块

**Status:** done

**Blocked by:** 无——可立即开始

**构建内容：** `fs_read` 与 `cat` 共享统一读文件链路（判目录 → 读文件 → 字节范围切片 → 解码 → 行范围切片），行范围对结尾换行文件的行为一致；EISDIR/ENOENT 语义与编码行为单一来源。

**验收标准：**

- [ ] `fs_read` 与 `cat` 行范围读取对同一文件结果一致（含以换行结尾的文件）
- [ ] 字节范围（解码前切片）与行范围（解码后切片）语义统一
- [ ] `fs_read` 输出契约字段（content/truncated/lines）形状不变；行范围对结尾换行文件按 splitLines 语义统一（PRD 决策 3 预期行为，CHANGELOG 已记录）
- [ ] EISDIR 文案统一为「是目录而非文件」（PRD 决策 3：统一判目录语义；spec 审查确认属预期，CHANGELOG 已记录）
- [ ] 行范围行为变化已记 CHANGELOG（0.x 窗口，ADR-0007）
- [ ] 单测覆盖：字节/行范围组合、EISDIR/ENOENT、结尾换行边界

## 评论

来源：PRD 决策 3（P2）。共享读文本模块的抽取此前只服务 text_grep 族；本工单使其成为读文件深模块。
