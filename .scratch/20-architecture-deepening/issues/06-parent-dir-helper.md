# 06 · 父目录预检助手

**Status:** done

**Blocked by:** 无——可立即开始

**构建内容：** `fs_write` 与 `net_download` 共享同一父目录预检语义：父路径存在且为目录即通过，存在但非目录返回 `ENOTDIR`，不存在且允许建时递归创建，不存在且不允许时返回 `ENOENT`——同一前置条件两种工具错误码与文案完全一致。

**验收标准：**

- [ ] `fs_write` 与 `net_download` 共用同一父目录预检实现，本地重复逻辑删除
- [ ] 三态（通过 / ENOTDIR / 递归建 / ENOENT）错误码与文案对齐现状
- [ ] `mkdirParents` 缺省行为不变（两工具均默认 true）
- [ ] 助手级单测覆盖四态 + 两调用方行为回归

## 评论

来源：PRD 决策 6（P2）。
