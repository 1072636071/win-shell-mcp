# 追加引导语义的护栏断言（与 08 号元数据护栏同址）

**Status:** ready-for-agent

**Blocked by:** 01——本工单断言的对象（改写后的 description）由 01 产出，需在 01 落地后运行

**构建内容：** 元数据护栏测试在断言"description 非空且 ≤ 预算"的基础上，额外钉住一条外部可观察事实：`batch_run` 的描述确实包含主动引导语义——即包含"优先"与"一次"两个引导关键词，使"多步操作优先一次完成"的引导在后续任何人对描述做精简/重写时不会悄悄丢失。它只锁语义、不快照全文措辞，从而与 08 号工单的护栏同址共存、不重复建测试文件。

**验收标准：**

- [ ] 在 08 号工单的元数据护栏测试文件内（同址），追加一组针对 `batch_run` description 的断言
- [ ] 断言 `batch_run` 的 description 包含引导关键词"优先"与"一次"（钉住"引导存在"这一语义，不快照全文）
- [ ] 断言 description 包含场景要素（读文件/替换/写回 一类流程示例）——以最小关键词方式断言，不锁全文
- [ ] 断言改写后的 description 长度满足 08 号工单预算护栏（≤ 软上限，或 08 号工单将 `batch_run` 从例外清单移除后回到 ≤150）
- [ ] 本工单只追加断言，不新建测试文件、不改动护栏的预算常量与遍历结构
- [ ] 断言方式沿用既有先例（经 `listTools()` 投影断言外部可观察形态，不测实现细节、不快照措辞全文）

## 评论

（评论与对话历史追加于此，新内容置于最前。）

PRD 测试决策说明：好测试的标准是"用最小关键词断言引导存在，但不锁死全文措辞"；`listTools()` 是既有 seam；现有测试对 description 仅有 `length > 0` 断言（`tests/server.test.ts`），本工单把"关键语义存在"提升为可断言项但保持克制。
