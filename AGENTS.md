# AGENTS.md

## Agent skills

### Issue tracker

本仓库使用本地 markdown issue tracker（`.scratch/` 下）。参见 `docs/agents/issue-tracker.md`。

### triage 标签

五个标准 triage 角色均使用其名称作为标签字符串。参见 `docs/agents/triage-labels.md`。

### 领域文档

单一上下文布局：根目录 `CONTEXT.md` + `docs/adr/`。参见 `docs/agents/domain.md`。
